import React from "react";

const Test = ({coba, paraf}) => {
    return(
        <div>
            <h1>{coba}</h1>
            <p>{paraf}</p>
            {/* {product.map((item)=>
            <li>{item.name}</li>
            )} */}
        </div>
    )
}

export default Test